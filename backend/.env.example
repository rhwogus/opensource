# macOS often uses port 5000 for AirPlay, so 5001 is safer.
PORT=5001

# SQLite
SQLITE_DB_PATH=./data/recifridge.sqlite

# GPT - put the real key only in backend/.env
OPENAI_API_KEY=sk-proj-paste_your_key_here
OPENAI_MODEL=gpt-4o-mini
