# macOS는 5000 포트를 AirPlay가 쓰는 경우가 많아 5001 권장
PORT=5001

# MySQL 
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=recifridge

# GPT  — 실제 키는 .env 에만 넣기
OPENAI_API_KEY=sk-proj-여기에_발급받은_키_붙여넣기
OPENAI_MODEL=gpt-4o-mini
